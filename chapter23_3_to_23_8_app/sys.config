[{kernel,
  [{logger,
    [{handler, default, logger_std_h, #{
        config => #{
          file => "error.log",
          max_no_files => 10, % Default
          max_no_bytes => 10485760 % 10 MB Default
        }
      }
    }]
  }]
}].
